apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: c36-cluster
  region: us-east-1
  version: "1.20"
nodeGroups:
  - name: ng-1
    instanceType: t2.medium
    desiredCapacity: 2

availabilityZones: ['us-east-1a','us-east-1b']
